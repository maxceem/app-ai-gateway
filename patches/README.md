`@maxceem/cf-auth@0.5.0` is patched with atomic registration admission,
password-change session revocation, and transactional credential checks pending
a cf-auth release. Remove the patch and this note after upgrading to a release
that contains those capabilities.
